{
  "name": "redis_http_proxy",
  "version": "1.0.0",
  "description": "HTTP API for Redis Client",
  "main": "index.js",
  "scripts": {
    "start": "node main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "logist322",
  "license": "ISC",
  "dependencies": {
    "@redis/client": "^1.5.14"
  }
}
